type Repo = record { cid : text; owner : text; name : text; timestamp : nat64 };
service : {
  get_repo : (text) -> (opt Repo) query;
  get_user_repos : (text) -> (vec Repo) query;
  store_repo : (text, text) -> (text);
}
